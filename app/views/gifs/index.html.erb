<h1>Brighten up your day with these GIFs</h1>

<section class='gifs'>
  <% @category.gifs.each do |gif| %>
      <img src="<%=  gif.url %>"/>
      <%= button_to "Add to Favorites", user_favorites_path(user_id: current_user.id, gif_id: gif.id )  %>
    <% end %>
</section>