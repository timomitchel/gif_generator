<h1>Your Favorites</h1>

<section class='favorites'>
  <% @favorites.each do |favorite|  %>
  <img src="<%= favorite.find_gif_by_id%>">
  <%= link_to 'remove favorite', user_favorite_path(id: favorite.id, user_id: current_user.id, gif_id: favorite.gif_id), method: :delete %>
</section>
<%end %>